setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

release:
  command:
    - python manage.py migrate

run:
  web: gunicorn AcheiUnB.wsgi:application --bind 0.0.0.0:$PORT
